const grids = document.querySelectorAll('.grid');
const headings = document.querySelectorAll('.heading .wrapper .text');

function enterScreen(index) {}
function exitScreen(index, exiteDelay) {}

function SetupAnimationCycle({initalScreenIndex, timePerScreen, exitDelay}){

}

SetupAnimationCycle({
    initalScreenIndex:0,
    timePerScreen:2000,
    exitDelay: 200 * 7
})